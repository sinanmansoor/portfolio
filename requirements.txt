Django==5.1.1
djangorestframework==3.15.2
django-cors-headers==4.4.0
gunicorn==23.0.0
psycopg[binary]>=3.1.0
whitenoise>=6.6.0

# ── AI / RAG layer ──────────────────────────────────────────────────────────
# Local embedding model (all-MiniLM-L6-v2) — free, no API key required
sentence-transformers>=3.0.0
# In-process vector store — stores & queries text chunk embeddings
chromadb>=0.5.0
# Grok (xAI) uses the OpenAI-compatible API — this SDK handles streaming too
openai>=1.40.0